{{#each erros}}
    <div class="erro">{{texto}}</div>
{{else}}

{{/each}}

<h3 class="tit_h3">Editar Fornecedor</h3>
<form action="/usuarios/fornecedor/edit" method="post" class="form-fornecedor">
    <input type="hidden" name="id" value="{{fornecedor._id}}">
    <label for="nome" class="form-label">Nome: </label>
    <input type="text" name="nome" id="nome" placeholder="Nome" value="{{fornecedor.nome}}">
    <label for="telefone" class="form-label">Telefone: </label>
    <input type="text" name="telefone" id="telefone" placeholder="telefone" value="{{fornecedor.telefone}}">
    <button type="submit" class="botao">Editar Fornecedor</button>
</form>