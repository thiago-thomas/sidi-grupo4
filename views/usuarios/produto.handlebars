<div class="logo logo_dash">
    <a href="/usuarios"><img src="/imgs/logo.jpeg" alt="Logo Rei do Cangaço"></a>
    <div class="nome_novo">
        <span>Produtos</span>
        <a href="/usuarios/produto/add"><button class="botao botao_nome_novo">Novo Produto</button></a>
    </div>
</div>

<div class="dashboard">
    {{#each produto}}
    <div class="campo_obj">
        <div>
            <h3>Nome: {{nome}}</h3>
            <h3>Valor: {{valor}}</h3>
        </div>
        <div>
            <a href="/usuarios/produto/edit/{{_id}}"><button class="botao btn_tam">Editar</button></a>
            <form action="/usuarios/produto/del" method="post">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="error btn_tam">Deletar</button>
            </form>
        </div>

    </div>
    {{else}}
    <h4>Nenhum Produto Registrado</h4>
    {{/each}}
</div>